<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sunrun.mapper.DevExclusiveSwitchboardConnMapper">

	<insert id="saveSwitchboardConn">
		insert into dev_exclusive_switchboard_conn(
			id,					exclusive_switchboard_ip,	exclusive_switchboard_port,			exclusive_switchboard_info,		brand_name,			
			model_name,			currentIos_version,			create_time,						state,							create_user
		) values(
			#{conn.id},			#{conn.exclusiveSwitchboardIp},	#{conn.exclusiveSwitchboardPort},#{conn.exclusiveSwitchboardInfo}, #{conn.brandName},				
			#{conn.modelName},	#{conn.currentIosVersion},	now(),							 	1,								#{conn.createUser}
		)
	</insert>
	

	<select id="findSwitchboardConn" resultType="com.sunrun.entity.DevExclusiveSwitchboardConn">
		select * from dev_exclusive_switchboard_conn  where state=1
		<if test="id != null">
			id = #{id}
		</if>
	</select>

	

</mapper>